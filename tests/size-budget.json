{
  "comment": "Line budgets per file type, enforced by tests/checks/size.test.js. soft = a warning that a split is due; hard = a failure, split before extending. Line count is only a PROXY for what actually degrades a reader, which is one concept stated in two places saying slightly different things - and that is enforced directly by the one-home gate. Where the two disagree, the one-home gate wins: a longer single file beats the same rule restated in three shorter ones.",
  "why-these-numbers": "Source files sit at 400/600 so one read-file action captures a whole module cheaply. The previous 300/500 produced 41 per-file exceptions across 39 files - a registry of exemptions rather than a constraint, each carrying an exit plan nobody took. Raising the cap to what the repo had actually settled at, and requiring an acknowledgement rather than a bespoke cap, took that to one exception and fifteen acknowledgements.",
  "byExtension": {
    ".js": {
      "soft": 400,
      "hard": 600
    },
    ".ts": {
      "soft": 400,
      "hard": 600
    },
    ".css": {
      "soft": 400,
      "hard": 600
    },
    ".html": {
      "soft": 300,
      "hard": 450
    },
    ".sql": {
      "soft": 400,
      "hard": 600
    },
    ".md": {
      "soft": 400,
      "hard": 600
    }
  },
  "generated": [
    "docs/CODEMAP.md",
    "llms.txt",
    "supabase/schema-snapshot.json",
    "tests/surface-baseline.json"
  ],
  "exempt-trees": {
    "docs/sessions-archive/": "Closed, read-only history. Never reflowed to a budget.",
    "supabase/migrations/": "Applied migrations are immutable once run (CLAUDE.md). Their length tracks the schema they create, and editing one to fit a budget would rewrite history the database has already applied."
  },
  "exceptions": {
    "docs/STATE.md": {
      "hard": 40,
      "note": "TIGHTER than its extension, not looser. A fixed-size current state, overwritten each checkpoint and never grown. The 40-line cap is the point of the file."
    }
  },
  "acknowledged": {
    "docs/CHANGELOG.md": {
      "seam": "Do not split: it would put 'what changed' in two places. It shrinks by RELEASE, not by split - Unreleased becomes a dated heading and the file resets. Read newest-first, so length costs a reader nothing."
    },
    "supabase/seed.sql": {
      "seam": "Do not split while it is one readable pass of sample rows. Length tracks the number of rows, not complexity. If it nears the hard cap, split per domain into a seed/ directory."
    },
    "supabase/policies.sql": {
      "seam": "Never split. This is the security boundary and 'which tables can be written, and by whom' must stay a single-pass question. It grows with every new table, so it is the one file here likely to reach the cap - when it does, raise the cap and say why, do not fragment the answer."
    },
    "assets/css/components.css": {
      "seam": "Not worth splitting: it loads on all 24 pages, so shedding badge and table styles into components-data.css would add a request to every one of them to save 78 lines. Revisit only if it passes 550."
    },
    "tests/unit/roadmap/views.test.js": {
      "seam": "Not worth splitting: the placement-ordering cases share one fixture with the rest, so a split duplicates the fixture. No runtime cost either way, so the only gain would be cosmetic. Revisit if the fixture itself is extracted."
    },
    "docs/plan/80-LOAD-SPEED.md": {
      "seam": "Closed workstream record. Most of the length is the account of where the plan was wrong, which is the part a later wave needs. Folds into a short outcome at the next release, not before."
    },
    "assets/js/pages/roadmap/roadmap.js": {
      "seam": "Not yet. The roadmap page already loads twelve modules and is the heaviest page in the site; a roadmap-fetch.js would make it thirteen. The page-load fetch and the phase/link enrichment in onAuthed are the seam when it passes 550."
    },
    "supabase/schema/30_work.sql": {
      "seam": "The work-items domain in one file, most of its length being the rationale beside each constraint. work_documents and work_notes are the seam - they are intake concerns, not roadmap ones - when it passes 550."
    },
    "assets/css/tokens.css": {
      "seam": "Do not split. Tokens plus the dark-theme overrides: inherently long, no logic, and every value in the design system resolves here. A tokens-dark.css would add a request to all 24 pages for no reading gain."
    },
    "assets/css/roadmap-views.css": {
      "seam": "Not yet, and the closest of the stylesheets to a real seam: six board layouts share one coordinate model, so splitting means duplicating that model or inventing a shared file that is most of it. Revisit when a seventh view is added, or at 550 - whichever comes first."
    },
    "assets/js/pages/roadmap/detail.js": {
      "seam": "Not yet. The value helpers already left for detail-values.js, which is what unfroze this file. The details and notes parsers are the next seam, at 550."
    },
    "docs/ROADMAP-INTAKE.md": {
      "seam": "Never split. It is the ONE home for the confidence bands, and one-home.test.js fails if they are restated elsewhere. The last attempt to split this file is what put them in three places."
    },
    "assets/js/pages/roadmap/views.js": {
      "seam": "Not yet. The four layout families are already separate files; what is left is the shared placement model they all read. Splitting it further means another request on the heaviest page. The context and placement helpers are the seam at 550."
    },
    "docs/plan/20-API-REFERENCE.md": {
      "seam": "Closed workstream record. Folds into a short outcome at the next release, keeping the corrections table."
    },
    "assets/css/roadmap.css": {
      "seam": "Not yet. The print @media block is self-contained and would move cleanly, but it would cost the roadmap page a request. At 550, take it."
    },
    "docs/plan/101-DECISIONS.md": {
      "seam": "A register: it grows by one entry per decision and shrinks only when decisions are superseded, as D14 and D21 already have been. Do not split by session or theme - the whole value is that one number resolves in one place. Revisit at 550: past that, move superseded decisions to a closed section at the foot rather than fragmenting the live ones. Folds to a short outcome record once Session B closes."
    },
    "assets/css/roadmap-detail.css": {
      "seam": "Not yet. It is the one stylesheet for the roadmap drawer and the executive board, loaded only by modules/roadmap/, so splitting it would add a request to the heaviest page in the site to save three lines. The seam when it passes 550 is the executive board rules (.rmd-exec*, the department-first rollup) leaving for roadmap-exec.css, since the board and the drawer share no selectors."
    }
  },
  "why-acknowledgements-are-decisions": "An acknowledgement is a DECISION not to split, with the condition that would change it - not a queued task. Every one below was re-judged on 2026-08-30 against a question the earlier exit plans did not ask: what does the split COST? Splitting a stylesheet or a page module adds an HTTP request to every page that loads it, and this repo has no build step to hide that. The same session had just removed 21 requests by merging two nav tools; reintroducing them to shorten a file that sits 120 lines under its hard cap would be a bad trade made for tidiness. So the answer for all fifteen is currently 'not yet', each with the trigger that would change it. Reaching the hard cap is always such a trigger."
}
