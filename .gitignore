# Local configuration containing Supabase keys. Never commit.
assets/js/core/config.js
assets/js/config.js

# Environment files
.env
.env.*

# OS and editor noise
.DS_Store
Thumbs.db
*.swp

# Dependencies (if tooling is ever added)
node_modules/

# Local scratch files
*.local.*
scratch/

# Claude Code personal overrides (the shared policy is .claude/settings.json,
# which IS committed; settings.local.json is per-developer and stays local).
.claude/settings.local.json
